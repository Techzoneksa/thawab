# نشر ثواب كـ SaaS متعدد المستأجرين

هذا الدليل يشرح تشغيل ثواب كمنتج تبيعه لعدة جمعيات، كل جمعية **معزولة تماماً**
عن غيرها، من **تطبيق واحد** على سيرفر واحد + **قاعدة Neon مستقلة لكل جمعية**.

الجمعية الحالية `thawab.jaadpro.com` (خالد) **تبقى كما هي** على Supabase — هذا
النظام للجمعيات الجديدة فقط، ولا يؤثر عليها إطلاقاً.

---

## المعمارية

```
   Neon (قواعد مُدارة)             VPS واحد (الحوسبة)
   ┌──────────────┐               ┌────────────────────────────┐
   │ قاعدة radifa │◄──────────────┤ تطبيق ثواب واحد             │
   │ قاعدة abufadi│◄──────────────┤ (متعدد المستأجرين)          │
   │ قاعدة ...    │◄──────────────┤ Nginx + PM2                │
   └──────────────┘               │ + مواقعك وبرامجك الأخرى     │
                                   └────────────────────────────┘
```

- **التوجيه:** التطبيق يقرأ النطاق الفرعي من كل طلب (`radifa.jaadpro.com` → `radifa`)
  ويستخدم قاعدة ذلك المستأجر. لا يوجد بيانات مشتركة بين الجمعيات.
- **العزل:** مضمون فيزيائياً — كل جمعية على قاعدة Neon منفصلة.
- **الإعداد الذاتي:** أول زائر لقاعدة فارغة يُوجَّه إلى `/setup` لإنشاء حساب
  المدير وكلمة مروره بنفسه.

---

## متغيرات البيئة (على التطبيق)

| المتغير | الغرض | مثال |
|---|---|---|
| `TENANTS_JSON` | سجل المستأجرين: نطاق فرعي → رابط قاعدته | `{"radifa":{"databaseUrl":"postgres://…neon…"},"abufadi":{"databaseUrl":"…"}}` |
| `BASE_DOMAIN` | النطاق الأساسي لاستخراج النطاق الفرعي | `jaadpro.com` |
| `SESSION_COOKIE_SECURE` | كوكي آمن في الإنتاج | `true` |
| `NODE_ENV` | البيئة | `production` |
| `APP_URL` | (اختياري) للروابط في الإيميل | `https://radifa.jaadpro.com` |
| `SMTP_*` | (اختياري) لدعوات المستخدمين | راجع `.env.example` |

> **`DATABASE_URL` غير مطلوب** في وضع تعدّد المستأجرين — كل مستأجر يجلب رابطه من
> `TENANTS_JSON`. لو لم يُضبط، يتخطّى التطبيق ترحيلات الإقلاع بأمان.
>
> **السرّية:** روابط القواعد تعيش في متغير البيئة فقط، لا في الكود ولا في Git.

---

## الإعداد لمرة واحدة (VPS)

1. **Node.js 22** + **Nginx** + **PM2** (`npm i -g pm2`).
2. انشر الكود (فرع `nextjs-migration` أو ما تعتمده)، ثم `npm ci && npm run build`.
3. **DNS:** سجل wildcard `*.jaadpro.com → عنوان الـ VPS` (يغطّي كل نطاق فرعي جديد).
4. **SSL:** شهادة wildcard واحدة `*.jaadpro.com` (Let's Encrypt / certbot DNS-01).
5. **Nginx:** وجّه كل النطاقات الفرعية إلى منفذ التطبيق (مثال `server_name *.jaadpro.com;`
   → `proxy_pass http://127.0.0.1:3000;`).
6. شغّل التطبيق عبر PM2 مع متغيرات البيئة أعلاه.

---

## إضافة جمعية جديدة (مثال: أبو فادي)

1. **في Neon:** أنشئ قاعدة جديدة `abufadi` (تحت مشروعك) وخذ رابط الاتصال (Pooled).
2. **جهّز القاعدة** من مجلد المشروع:
   ```bash
   bash scripts/new-tenant.sh abufadi "postgresql://…neon…/abufadi?sslmode=require"
   ```
   (يطبّق كامل الـ schema — قاعدة فارغة جاهزة.)
3. **سجّل المستأجر:** أضف السطر المطبوع إلى `TENANTS_JSON` ثم أعد تشغيل التطبيق:
   ```
   "abufadi": { "databaseUrl": "postgresql://…neon…/abufadi?sslmode=require" }
   ```
4. **DNS:** موجود تلقائياً عبر الـ wildcard — لا شيء إضافي.
5. **التسليم:** أرسل للجمعية `https://abufadi.jaadpro.com`. يفتحونه، وشاشة الإعداد
   الأول تُنشئ حساب مديرهم وكلمة مروره. خلص.

> **التحديثات:** الكود واحد للجميع — أعد النشر مرة واحدة. لو التحديث فيه تغيير
> schema، شغّل `db:migrate` على قاعدة كل مستأجر (كرّر الخطوة 2 بدون بيانات جديدة).

---

## النسخ الاحتياطي (غير قابل للتفاوض)

بيانات مالية = نسخ احتياطي إلزامي. Neon يوفّر **استرجاع نقطة-زمنية (PITR)** مُدار،
لكن يُنصح بنسخة يومية مستقلة (`pg_dump` لكل قاعدة → تخزين خارجي) واختبار الاسترجاع
دورياً.

---

## الاختبار

اختبار التوجيه والعزل على قاعدتين حقيقيتين:
```bash
TENANT_A_URL=postgres://… TENANT_B_URL=postgres://… \
  node_modules/.bin/tsx scripts/smoke-tenant.ts
```
