# تقرير الفحص الشامل — ثواب (Thawab)

> فحص أمني + معماري + محاسبي، مُجمّع من 3 مراجعات مستقلة. **فحص فقط — لم يُعدّل أي كود.**
> التاريخ: 2026-08-11

---

## الخلاصة التنفيذية

المشروع فيه **أساس محاسبي سليم مفاجئ** (محرّك قيد مزدوج حقيقي يفرض التوازن، وتقارير تُحسب من القيود المرحّلة)، لكنه **غير صالح للإنتاج إطلاقاً** حالياً بسبب ثلاث فئات مشاكل قاتلة تعمل بالتوازي:

1. **أمنياً**: لا توجد مصادقة/تفويض على أي endpoint تقريباً — أي شخص يقدر يقرأ/يعدّل/يحذف كل البيانات المالية وبيانات المستفيدين بدون تسجيل دخول. كلمات المرور `base64` (شبه نص صريح)، توكِن الجلسة قابل للتخمين، وهوية سجل التدقيق قابلة للتزوير.
2. **معمارياً**: تلف ترميز (mojibake) في 21 من 25 ملف API يكسر مقارنات الحالة (يمنع ترحيل أي قيد فعلياً)، لا توجد transactions حول الكتابات المالية متعددة الخطوات، ومسار Postgres مكسور بالكامل.
3. **محاسبياً**: العمليات (تبرعات/مساعدات/سندات) **غير مربوطة بالدفتر العام** — الفلوس ما توصل الحسابات، وشجرة الحسابات مصنّفة خطأ فتطلع القوائم المالية فاضية.

بالإضافة إلى أن **الواجهة تشتغل غالباً على بيانات وهمية** (`src/data/sample.ts`) وليست مربوطة بالـ API الحقيقي في أغلب الصفحات.

---

## 🔴 CRITICAL — يجب إصلاحها قبل أي شيء

### الأمن

| # | المشكلة | الملف | الأثر |
|---|---------|-------|-------|
| S1 | **لا مصادقة ولا تفويض على كل الـ endpoints** عدا `/api/auth`. `safeHandler` يعمل init + التقاط أخطاء فقط، ولا يتحقق من الجلسة. | كل `src/routes/api/**` عدا auth، عبر `src/server/db/api-utils.ts:3-16` | `curl -X DELETE /api/donations?id=...` يحذف تبرعاً بدون دخول؛ `GET /api/beneficiaries` يسحب سجل المستفيدين بأرقام الهوية. |
| S2 | **كلمات المرور base64 مش hash** — قابلة للفك فوراً. | `src/server/db/auth.ts:5-11`, مكرر في `seed.ts:34-40` | تسريب/نسخة احتياطية للـ DB = كل كلمات المرور مكشوفة. |
| S3 | **توكِن جلسة غير عشوائي تشفيرياً** — `Date.now()+Math.random().substr(2,5)`. | `auth.ts:13-14` + `index.ts:50-53` | تخمين/تعداد التوكِن = اختطاف جلسة. |
| S4 | **هوية سجل التدقيق قابلة للتزوير** — `userId`/`userName` تؤخذ من body/query الطلب. | كل معالجات الكتابة، مثال `donors.ts:170-171` | تلفيق عمليات باسم الأدمن؛ سجل التدقيق عديم القيمة. |
| S5 | **`/api/diagnose` يكشف `DATABASE_URL`** (يوزر/باسورد قاعدة الإنتاج) بدون حماية. | `src/routes/api/diagnose.ts:4-16` → `client.ts:148` | `GET /api/diagnose` = اتصال مباشر بقاعدة الإنتاج. |

### المعمارية

| # | المشكلة | الملف | الأثر |
|---|---------|-------|-------|
| A1 | **تلف ترميز (mojibake)** في 21/25 ملف API — النصوص العربية للحالات محفوظة كرموز مكسورة (`"ظ†ط´ط·"` بدل `"نشط"`)، بينما schema/seed سليمة. | كل `src/routes/api/**` عدا auth/health/diagnose/receipts | `account.status !== "ظ†ط´ط·"` دايماً true ← **كل ترحيل قيد يُرفض**؛ وكل فلاتر الحالة مكسورة. |
| A2 | **لا transactions** حول أي كتابة مالية متعددة الخطوات (صفر `db.transaction`). | `journal.ts` (create/reverse/update)، `donations.ts:122-179` | فشل في المنتصف = قيد يتيم/غير متوازن، أو `donors.totalDonations` تنفصل عن الواقع. |
| A3 | **مسار Postgres مكسور على 4 محاور**: (1) `dialect==="postgres"` دايماً false لأنه Proxy؛ (2) `SQLITE_DDL` يُنفَّذ حتى على PG؛ (3) الـ routes تستورد schema الـ SQLite دائماً؛ (4) `.all()/.run()/.get()` غير موجودة في drizzle postgres-js (514 استدعاء يفشل). | `index.ts:18-24`, `client.ts:119-123`, `schema.ts` | Postgres لا يعمل إطلاقاً اليوم — "dual-dialect" وهمي. |

### المحاسبة

| # | المشكلة | الملف | الأثر |
|---|---------|-------|-------|
| F1 | **شجرة الحسابات كلها `type="تفصيلي"`** بينما التقارير تفلتر على أصل/التزام/إيراد/مصروف. | `seed.ts:194-295` vs `statements.ts` | **قائمة الدخل والمركز المالي تطلع فاضية.** |
| F2 | **عمود `type` يخلط التصنيف مع الطبيعة** (رئيسي/تفصيلي). الطبيعة مغطّاة أصلاً بـ `postable`. | `accounts.ts:7-15` | السبب الجذري لـ F1. |
| F3 | **العمليات مفصولة تماماً عن الدفتر العام** — تأكيد تبرع/صرف مساعدة/سند قبض **لا يُنشئ أي قيد**. | `donations.ts:86-194`, `aid.ts`, `receipts.ts`, procurement | الصندوق/البنك والإيرادات ما توصلها فلوس ← الدفتر فاضي في الواقع. |
| F4 | **إقفال الفترة لا يمنع الترحيل داخلها** — مجرد تبديل حالة. | `periods.ts` vs `journal.ts` | الإقفال غير قابل للفرض. |

---

## 🟠 HIGH

- **H1 — `.map(async …)` بدون `await`/`Promise.all`**: endpoints القوائم ترجع `[{}, {}]` (الحقول المثراة تضيع). `journal.ts:39,132`, `aid.ts:72`, `receipts.ts:17`, `budgets.ts:37`, `statements.ts:259` (تقرير الموازنة مقابل الفعلي = NaN).
- **H2 — لا zod validation** على أي mutation؛ أرقام تُحوّل بصمت، لا حد لحجم الـ payload (DoS).
- **H3 — بيانات دخول أدمن مزروعة (`admin123`) ومعروضة في صفحة اللوقن**، والـ seed يشتغل تلقائياً على الاستيراد. `seed.ts:136-191`, `login.tsx:158-171`.
- **H4 — `generateJournalNumber` سباق/غير ذرّي** — أرقام قيود مكررة تحت التزامن (يفشل UNIQUE). `journal.ts:667-681`.
- **H5 — الجلسة في `localStorage`** وتُرسل كـ header — عرضة للسرقة عبر XSS؛ لا httpOnly/Secure/SameSite. `auth.tsx:38-64`.
- **H6 — لا rate-limiting/lockout على اللوقن**، ورسائل خطأ تكشف وجود المستخدم، ومقارنة كلمة المرور غير ثابتة الزمن.

## 🟡 MEDIUM / LOW (مختصر)

- **M1** — التواريخ تُخزَّن كنص locale عربي (`toLocaleString`) ← الترتيب الزمني مكسور. لازم ISO-8601. `index.ts:46-48`.
- **M2** — migrations خام (`ALTER TABLE ADD COLUMN` غير idempotent) مع ابتلاع الأخطاء؛ init فاشل يُخزَّن ولا يُعاد. drizzle-kit موجود لكن غير مستخدم.
- **M3** — نموذج القيد مكرر: `debitAccount/creditAccount/amount` على الهيدر + `journal_lines` = مصدرا حقيقة متنافسان.
- **M4** — `accounts.balance` لا يُحدَّث عند الترحيل (عمود ميت/مضلل)؛ N+1 واسع في الإثراء.
- **M5** — لا CSRF/CORS/security headers؛ لا CSP. `start.ts`.
- **F5 (محاسبي)** — لا قيود إقفال سنوية ولا حساب أرباح مبقاة/صافي أصول؛ الأرصدة الافتتاحية يتيمة (بلا قيود، ولا تظهر بالتقارير).
- **F6 (محاسبي)** — **الأموال المقيّدة مقابل غير المقيّدة** مجرد نص حر على الهيدر، لا فصل حقيقي ولا تقرير (جوهري للجمعيات).
- **F7 (محاسبي)** — **لا كيان فاتورة إطلاقاً** ولا حقول ZATCA (ضريبة/QR/UUID/رقم ضريبي بائع-مشتري).
- **L** — `schema.ts` نسخة طبق الأصل من `schema.sqlite.ts`؛ Proxy hazards في `client.ts`؛ BOM في `auth.ts`؛ مخلّفات build في الشجرة (`deploy.zip` 7.7MB، `.output/` 32MB، `server/` 14MB).

---

## ✅ الأشياء السليمة فعلاً (نبني عليها)

- محرّك القيد المزدوج يفرض التوازن (مدين=دائن) عند الإنشاء والترحيل والتعديل، ويولّد قيد عكسي صحيح.
- القوائم المالية (ميزان المراجعة، قائمة الأنشطة، المركز المالي) تُحسب بالتجميع من `journal_lines` المرحّلة — المنهج الصحيح.
- دفتر الأستاذ يحسب الرصيد الجاري والرصيد الافتتاحي ويستبعد القيود العكسية.
- إقفال الفترة يمنع القيود المسودّة ويتحقق من توازن كل قيد مرحّل.
- schema الـ SQLite و PG **متطابقان بنيوياً** (37 جدول، تحويل أنواع صحيح)؛ init DDL يطابقهما.
- اصطلاح TanStack Start server-routes صحيح وموحّد عبر كل الملفات.
- `.gitignore` سليم — لا `.env` ولا `.db` مرفوعة.

---

## جرد الديمو (Demo Inventory)

### 1) `src/data/sample.ts` (1,395 سطر) — يُقسَّم لا يُحذف
- **يُبقى ويُنقل** إلى `src/lib/format.ts`: `fmtSAR`, `fmtNum`, `fmtPct` — مستوردة في ~60 ملف. حذف الملف الآن يكسر التطبيق كله.
- **يُحذف** (مصفوفات وهمية): `ORG, TENANTS, KPIS, DONATIONS_BY_PROJECT, CASH_FLOW_12M, TOP_DONORS, RECENT_TRANSACTIONS, BANK_ACCOUNTS, APPROVALS, ALERTS, DONORS, DONATIONS, PROJECTS, BENEFICIARIES, CHART_OF_ACCOUNTS, JOURNAL_ENTRIES, PURCHASE_REQUESTS, SUPPLIERS, INVENTORY_ITEMS, ASSETS, EMPLOYEES, BOARD_MEMBERS, MEETINGS, GRANTS, ENDOWMENTS, CAMPAIGNS, AUDIT_LOG, PERMISSIONS_MATRIX, REPORT_CATEGORIES, INTEGRATIONS, WORKFLOWS`.

### 2) صفحات تعرض بيانات وهمية مباشرة (غير مربوطة بالـ DB) — تُحوَّل لـ API
`index.tsx` (لوحة التحكم كاملة)، `approvals`, `campaigns`, `endowments`, `endowment-returns`, `grants`, `hr`, `memberships`, `meetings`, `notifications`, `permissions`, `reports`, `workflows`, `settings.system`, `settings.integrations`, `donor-orgs`, `distribution`, `recurring`.

### 3) `src/server/db/seed.ts` — يُقلَّص لـ bootstrap فقط
- **يُبقى**: الأدوار (roles)، شجرة حسابات مُصنَّفة صحيحاً، **مستخدم أدمن واحد** (بـ hash حقيقي + إجبار تغيير كلمة المرور).
- **يُحذف**: كل المستخدمين والفروع والمتبرعين والحملات والمشاريع والتبرعات والمستفيدين والمساعدات والموردين والمخازن والمخزون ومراكز التكلفة والموافقات الوهمية + الأرصدة الافتتاحية المضمّنة.
- يُعاد تسميته `seedBootstrap()` ويُفصل عن أي demo خلف env flag.

---

## خطة الإصلاح المقترحة (مراحل)

**المرحلة 0 — قرارات** (تحتاج موافقتك، انظر أدناه): لهجة القاعدة، استراتيجية الـ enums، نطاق الفواتير/ZATCA.

**المرحلة 1 — الأساس**: إصلاح الترميز؛ توحيد اللهجة على Postgres + تبنّي drizzle-kit migrations؛ إضافة transactions؛ التواريخ ISO؛ `now()`/`genId` الصحيحة.

**المرحلة 2 — الأمن**: `requireAuth` + فحص الصلاحيات على كل endpoint؛ hash حقيقي (argon2/bcrypt)؛ توكِن CSPRNG؛ جلسة عبر cookie httpOnly/Secure/SameSite؛ الهوية من الجلسة فقط؛ إزالة تسريب diagnose + بيانات الدخول التجريبية + env-gate للـ seed؛ zod validation؛ rate-limit؛ security headers.

**المرحلة 3 — صحة المحاسبة**: فصل التصنيف عن الطبيعة؛ زرع شجرة حسابات جمعية قياسية صحيحة؛ **خدمة ترحيل تلقائي للدفتر** (تبرع/مساعدة/سند/مشتريات/رواتب)؛ فرض قفل الفترة؛ قيود الأرصدة الافتتاحية؛ الإقفال السنوي/الأرباح المبقاة؛ نموذج الأموال المقيّدة؛ إصلاح `.map(async)`؛ حذف أعمدة الهيدر القديمة؛ ترقيم ذرّي.

**المرحلة 4 — الفواتير/ZATCA** (إن لزم): كيان فاتورة + حقول الضريبة.

**المرحلة 5 — حذف الديمو**: تقسيم `sample.ts`؛ حذف المصفوفات؛ تحويل الصفحات الوهمية لـ API؛ تقليص الـ seed.

**المرحلة 6 — النظافة**: إزالة مخلّفات build؛ `.editorconfig` (UTF-8)؛ حارس CI ضد mojibake.
