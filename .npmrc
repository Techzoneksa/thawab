# Force devDependencies to install even when NODE_ENV=production,
# because the build toolchain (vite, @lovable.dev/vite-tanstack-config,
# drizzle-kit, etc.) lives in devDependencies and is required by `npm run build`.
include=dev
