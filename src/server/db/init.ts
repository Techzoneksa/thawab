/**
 * @deprecated Raw DDL/migration strings removed.
 * Schema migrations are now managed by drizzle-kit (see drizzle.config.ts, ./drizzle)
 * and applied via `ensureInit()` in ./index.ts.
 */
export {};
