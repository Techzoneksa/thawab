/**
 * @deprecated The demo seed was removed. Production bootstrap (roles, a
 * classified chart of accounts, one admin user, and an open fiscal period)
 * now lives in `scripts/seed.ts` and is run via `npm run seed`.
 */
export {};
