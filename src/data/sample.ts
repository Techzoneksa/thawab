// All demo/mock data has been removed for production.
// Formatters live in src/lib/format.ts and are re-exported here for
// backward compatibility with existing imports (`@/data/sample`).
export { fmtSAR, fmtNum, fmtPct } from "@/lib/format";
